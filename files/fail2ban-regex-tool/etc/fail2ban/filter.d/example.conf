# Fail2Ban filter for xxxx
#
#

[Definition]


failregex = ^\s[+-]\d{4} \S+ \d{3}0[1-9] \S+ <HOST>:\d+ [\d.]+:\d+ \d+ \d+ \d+\s

ignoreregex = 

# DEV Notes:
# this is how a typical filter.d file looks like
