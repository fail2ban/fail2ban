# Fail2Ban filter for gophernicus, a gopher server

[INCLUDES]

before = common.conf

[Definition]

_daemon = gophernicus

failregex = ^%(__prefix_line)serror ".*" for request ".*" from <HOST> (.*)$

ignoreregex =

# DEV NOTES:
# This is a syslog filter
#
# Author: Ruben Carlo Benante, git{hub,lab} @drbeco
# Created version 20220316

