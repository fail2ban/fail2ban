# Fail2Ban filter for Bitwarden
# Detecting failed login attempts
# Logged in bwdata/logs/identity/Identity/log.txt

[Definition]
failregex = ^\s*\[\w+\]\s+Failed login attempt(?:, 2FA invalid)?\. <HOST>$
